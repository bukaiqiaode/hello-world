pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'python --version'
            }
        stage('test') {
            steps {
                sh 'ls'
            }
        stage('deploy') {
            steps {
                sh 'ls -l'
            }
        }
    }
}
